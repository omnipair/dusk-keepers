{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/railway/rust.Dockerfile",
    "watchPatterns": [
      "Cargo.toml",
      "Cargo.lock",
      "protocol.lock.json",
      "rust/**",
      "deploy/railway/rust.Dockerfile"
    ]
  },
  "deploy": {
    "healthcheckPath": "/readyz",
    "healthcheckTimeout": 180,
    "restartPolicyType": "ALWAYS"
  }
}

